import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function InventoryPage() {
  return (
    <SectionPlaceholder
      title="Inventory"
      description="Inventory controls are scaffolded but not yet connected, so staff can navigate safely without being given misleading stock-editing actions."
    />
  );
}
