import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function StaffPage() {
  return (
    <SectionPlaceholder
      title="Staff"
      description="Staff assignments and shift coverage are intentionally left as a future phase so access control and operational ownership can be designed safely."
    />
  );
}
