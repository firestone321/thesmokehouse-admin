import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function MenuPage() {
  return (
    <SectionPlaceholder
      title="Menu"
      description="Menu administration will sit here once pricing, availability, and item-level validation rules are ready to be backed by live data."
    />
  );
}
