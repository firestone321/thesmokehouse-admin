import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function OrdersPage() {
  return (
    <SectionPlaceholder
      title="Orders"
      description="Detailed order management is intentionally held for the next phase so we can wire it safely with authentication, audit logging, and explicit state transition rules."
    />
  );
}
